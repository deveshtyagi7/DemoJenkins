# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DemoJenkins' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DemoJenkins
pod 'Firebase/Core'
	pod 'Firebase/Database'
	pod 'Firebase/Storage'
	pod 'Firebase/Auth'
 	pod 'SDWebImage', '~> 5.0'

  target 'DemoJenkinsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DemoJenkinsUITests' do
    # Pods for testing
  end

end
